apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: numbers-api
  labels:
    rng: hw
spec:
  selector:
    matchLabels:
      app: numbers-api
  template:
    metadata:
      labels:
        app: numbers-api
    spec:
      containers:
        - name: api
          image: kiamol/ch03-numbers-api
